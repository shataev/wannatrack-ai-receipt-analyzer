# Docker Compose for local run and deployment
# Usage: docker-compose up -d

services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    # Optional: override if needed
    # environment:
    #   - OPENAI_API_KEY=${OPENAI_API_KEY}
